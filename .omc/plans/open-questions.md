# Open Questions

## spatial-engine-v0.3 - 2026-05-11 (Round-3 update)

- [x] **v03-Q1 (CLOSED Round-3 by frozen contract)**: ADR 0010 §A1-ε line 53 + line 250 rejects all sidecar variants in v0.3 Linux fast path; D1-γ (no sidecar) is the only compliant option. D1-α (separate exe) and D1-β (subcommand) eliminated.
- [x] **v03-Q2 (DROPPED Round-3 — moot under A1-ε)**: no plugin↔sidecar channel exists; intra-plugin SPSC ring is internal (UDP I/O thread → audio callback).
- [x] **v03-Q3 (CLOSED Round-3)**: D3-γ hybrid chosen — state v3 reader at S2.5 day 1 (gets ~5 days of v0.2 fixture soak); writer + kMute param at S7 (isolated commit, debugging-blast-radius preserved). Pareto-dominates pure D3-α and pure D3-β.
- [ ] **v03-Q4 (D4 vendor-quirks real-data capture)**: Hard 60-day deadline + booked Korean lab session (D4-α, recommended) — Logistics: does user have specific lab venue in mind for the day-60 capture session? Cancellation fallback: if lab cancels within 7 days of booked date, ADR 0012 commit at day-60 is a "no-quirk-observed: synthetic fixture extension to day-90" note, and Notion task auto-files for day-90 re-booking.
- [x] **v03-Q5 (CLOSED Round-3)**: D5-β (defer macOS/Windows CI to v0.4) chosen.
- [x] **v03-Q6 (DROPPED Round-3 — moot under A1-ε)**: no systemd unit shipped; no sidecar daemon to auto-restart.
- [ ] **v03-Q7 (ADR 0010 §A5-β contingency activation criteria, v0.4+ deferrable)**: When does macOS/Windows port trigger fallback from A5-α to A5-β? Architect suggestion: "Activation criterion = single Apple/Steinberg/Avid forum thread OR cert-eval rejection citing `bind()` as cause." Defer concrete criterion to v0.4 sprint scoping.
- [ ] **v03-Q8 (NEW Round-3, Architect Round-2 §6.2)**: `IComponentHandler::performEdit` cross-thread safety — VST3 SDK does not document thread-safety for arbitrary worker threads. S2.6 SDK audit decides between (a) host message-thread queue marshaling, (b) `IRunLoop` (only if available without editor view — unexpected per ADR 0010 §A4-γ), (c) read-only state propagation via `restartComponent(kParamValuesChanged)`. Gates S4 entry. Commit footer in `vst3/SpatialEngineController.cpp` documents reasoning. — Why it matters: cross-thread `performEdit` UB = potential DAW crash on reverse path.
- [x] **v03-Q9 (NEW Round-3, Architect Round-2 §6.2 — CLOSED via design choice)**: registry stale-PID after reboot — embed `boot_id` (`/proc/sys/kernel/random/boot_id`) in each registry entry; writer-side GC drops entries with stale `boot_id` regardless of `/proc/{pid}/comm` match. Test added to `test_p_instances_registry.cpp` for stale-PID-after-reboot scenario.
- [x] **v03-Q10 (NEW Round-3, Architect Round-2 §6.2 — CLOSED via design choice)**: XDG_CONFIG_HOME empty-string semantics — `core/src/util/RegistryPath.h` handles `XDG_CONFIG_HOME=""` (set-but-empty) by falling back to `~/.config` per XDG spec; ~5 LOC fix; explicit unit test in `test_p_instances_registry`. Prevents silent-write to `./instances.json` in DAW cwd on locked-down corporate workstations.

## spatial-engine-phaseC3-adm-osc-compat - 2026-05-09

> **C3-Q1..Q10 cohort CLOSED via shipping in v0.2.0** (2026-05-10). ADM-OSC v1.0 dialect landed in `core/src/ipc/CommandDecoder.cpp:179+` as the A-β in-place extension; `--osc-dialect {legacy,adm}` CLI selector with `legacy` default ships in `core/src/bin/spatial_engine_core.cpp:391+`. v0.8 audit P4.2 (2026-05-29) closes the cohort with retrospective decisions below.

- [x] C3-Q1 — **CLOSED v0.2.0**: A-β shipped (extended `CommandDecoder` in-place; no separate listener). See `CommandDecoder.cpp:179+`.
- [x] C3-Q2 — **CLOSED v0.2.0**: B-α shipped (verbatim coexistence of `/obj/...` and `/adm/obj/...`, no alias map; last-write-wins on shared `obj_id`).
- [x] C3-Q3 — **CLOSED v0.2.0**: C-β shipped (VST3 ADM-OSC deferred). VST3 still uses legacy `/obj/...` only as of v0.7.0.
- [x] C3-Q4 — **CLOSED v0.2.0**: v0 must-haves shipped — `/adm/obj/N/{azim,elev,dist,aed,xyz,gain,mute,name}`; deferrals (`/adm/scene/*`, `/adm/config/*`, divergence/spread) remain unimplemented.
- [x] C3-Q5 — **CLOSED v0.2.0**: `legacy` default shipped (preserves wire-byte-equality tests). CLI flag `--osc-dialect adm` opts in.
- [x] C3-Q6 — **CLOSED v0.2.0** (HEAD-tracking): ADR 0006 cites the spec by URL without a pin. Re-evaluate at v1.x when consumer load justifies stability tradeoff.
- [x] C3-Q7 — **CLOSED via deferral**: synthetic fixtures shipped at S1. Real vendor captures = v03-Q4 60-day track, still open.
- [x] C3-Q8 — **CLOSED v0.2.0**: bridge-layer normalisation chosen — decoder treats `dist ∈ [0..1]` as canonical; metres-mode vendors (L-ISA) handled by adm_player → bridge transform.
- [x] C3-Q9 — **CLOSED v0.2.0**: `OSCBackend` `CommandSink` placement shipped (proposed). Dropped-cmd metric surfaced via `/sys/warning`.
- [x] C3-Q10 — **CLOSED v0.2.0**: `StateModel` accepts `seq=0` from ADM-tagged commands (decoder convention at `CommandDecoder.cpp:329-330` preserved). No StateModel code change required.

## spatial-engine-v1 - 2026-05-01

- [ ] F1: 3D dimensionality probe threshold (`1e-3` on `max(|speaker.y|)`) — should it be exposed as a `LayoutCompatibilityChecker` constant for consistency with v0 ADR M4? — Affects future layout-validation refactor; v1 keeps it private in `AlgorithmAnalyticReference.cpp`.
- [ ] F2: namespace placement for future HOA decoder — `core/src/ambi/` (sibling to encoder) or `core/src/render/` (co-located with VBAP/DBAP/WFS)? — Defer to v2 decoder spec.
- [ ] F3: `ElevationView` integration into `app.py` main window — this milestone or follow-up? — Decision: follow-up, to keep F3 scope minimal.
- [ ] F4: on hosts without `mido`, should `MidiBridge.start()` log a stderr warning or stay silent? — Decision: log to stderr (matches v0 backend-availability log style).

## spatial-engine-phaseC-C2 - 2026-05-05

- [ ] C2-Q1: JUCE pin version — 7.0.12 vs 7.0.x latest? — Phase B 검증된 minor 확인 필요. 너무 새 버전은 `juce_audio_plugin_client` API 변동 리스크.
- [ ] C2-Q2: MVP 의 6 파라미터 매핑 범위 — obj 0 만 노출 vs 다중 obj? — Phase D6 와 충돌 가능성 검토 필요. 현재 plan 은 obj 0 매핑 가정.
- [ ] C2-Q3: Generic editor 라벨 polish 를 C2 에 포함할지 Phase D6 까지 미룰지 — Reaper 의 6 슬라이더 라벨 사용자 혼동 가능.
- [ ] C2-Q4: pluginval strictness 5 fallback — D+9 시 strictness 5 fail 시 strictness 4 완화 vs C4 → D8 강등? — Phase C ADR Option C 와 상호작용 확정 필요.
- [ ] C2-Q5: VST3=ON CI job venue — `.github/workflows/vst3.yml` 신설 vs `.pre-commit-config.yaml` 로컬 hook? — 현재 repo 는 `.github/workflows/` 부재.
- [x] C2-Q6 (v2 결정, C-M3): `setLatencySamples(0)` 보고. 동적 propagation delay reporting 은 Phase D6 (D6.a) 검토.
- [ ] C2-Q7: D+2 bootstrap 실패 시 Option B 재활성화 의사결정자 — Architect 단독 vs Architect+Critic 합의? — 시간 손실 최소화를 위한 escalation 경로.
- [x] C2-Q8 (2026-05-06 결정): **JUCE 7 Educational tier 채택** (paik402@snu.ac.kr SNU 학술 사용). VST3 SDK 는 JUCE 번들 그대로 — Educational tier 가 GPLv3 fallback 자동 회피. C2 진행 전제 해소.

## spatial-engine-phaseC-C2-optionB - 2026-05-06

- [ ] C2-Q9 (재발현): VST3 SDK 라이선스 — Steinberg agreement 등록 vs GPLv3 fallback. spatial_engine 자체 라이선스 미정 시 GPLv3 fallback 가능한가? — Step 0.a entry 게이트 결정 의존.
- [ ] C2-Q10 (재정의): Option B 의 M2 strictness 게이트 후보 ① Steinberg validator (B.1) ② 자체 in-process VST3 host (B.2) ③ Phase D6 부채 회계 (B.3). 권장 라운드 1 draft = B.1 → B.3 fallback. — Architect/Critic 라운드 2 결정 필요.
- [ ] C2-Q11: Option B.2 채택 시 in-process host fixture 위치 — `vst3/tests/` (Driver #1 격리) vs `core/tests/` (host 코드 재사용). 권장 라운드 1 draft = `vst3/tests/`. — Architect 검토.
- [ ] C2-Q12 (신규): `process` audio thread 의 `inputParameterChanges` 를 control thread 로 forward 하는 layer 후보 ① component 측 std::thread ② host event-thread 신뢰 (controller→component IConnectionPoint) ③ Phase D6 부채. 권장 라운드 1 draft = ②. — SDK 문서 인용 검증 필요.
- [ ] C2-Q13: `room_preset_idx` (Dry/Small/Medium/Large) ↔ `PayloadReverbSelect` (fdn/ir) semantic mismatch — preset 매핑 테이블 신설 vs Phase D6 deferral. — Phase B reverb 설계 정합 검토.
- [ ] C2-Q14: `kSpatialEngineProcessor/ControllerUID` (128-bit CID) 자체 발급 (UUID v4) 시 호스트 충돌 가능성 — DEV prefix 표식 사용 여부, Phase D6 정식 등록 시 교체 정책.
- [x] C2-Q15 (2026-05-07 Step 0.c gate 발현, v5 amendment): v4 AM-R4-4 화이트리스트 fact-error 정정 — `futils.{cpp,h}` 가 vendored 트리에 부재 (실측 `core/JUCE/.../VST3_SDK/base/source/`: `baseiids.cpp` + `fbuffer{.cpp,.h}` + `fdebug{.cpp,.h}` + `fobject{.cpp,.h}` + `fstreamer{.cpp,.h}` + `fstring{.cpp,.h}` + `updatehandler{.cpp,.h}` + headers `classfactoryhelpers.h` / `fcommandline.h`, `futils` 부재). Step 1 helper 진입 default 강등: **option-β vtable-only path (helper 6개 미사용)** — `IComponent` + `IAudioProcessor` 직접 다중 상속 + `FUnknown` 직접 구현. 추후 helper 사용 재검토는 vendored 트리 실측 file list 기준 (`baseiids` / `fstreamer` 추가, `futils` 제거) 으로 재구성. — Step 0 commit footer 잠금.
- [x] C2-Q16 (2026-05-07 Step 1 link gate 발현, v6 amendment): plan §1 Principle 2 화이트리스트가 SDK base layer 누락. Step 1 link 단계에서 `Steinberg::FUnknown::iid` / `Steinberg::FUnknownPrivate::atomicAdd` / `Steinberg::IBStream::iid` undefined reference 발현 → `pluginterfaces/base/{funknown,coreiids}.cpp` 추가 link 필수. 두 파일은 file-level header 가 "Steinberg LICENSE file 종속" (BSD-3 표준 문구 부재) → Strand 1.b 분류 (Steinberg agreement OR GPLv3 dual-strand, GPLv3 fallback default). c2-licensing.md §"Strand 1.b" 잠금. plan §1 Principle 2 화이트리스트 확장 + §4 시나리오 3 별도 axis 분리 명시 권고. ODR fact-error 도 함께 발현 — wrapper 패턴 (INIT_CLASS_IID + #include vstinitiids.cpp) 이 SDK 헤더의 DECLARE_CLASS_IID 와 ODR 충돌. SDK 표준 패턴 = vstinitiids.cpp 를 INIT_CLASS_IID 없이 직접 컴파일 (헤더는 constexpr TUID 만 emit, vstinitiids.cpp 의 DEF_CLASS_IID 가 ClassName::iid emit, 1 link target 1회). wrapper 폐기. PIC 강제 추가 (top-level CMake: VST3=ON → CMAKE_POSITION_INDEPENDENT_CODE ON, OFF baseline 영향 0 — VST3=OFF default 분기). Step 1 commit footer 잠금.
- [x] C2-Q17 (2026-05-09 Step 3 fact-error, M1.c 발현): plan §6 Step 3.1 의 `IComponent::setIoMode(IoMode::kAdvancedKBypass)` enum 명이 vendored SDK (`pluginterfaces/vst/ivstcomponent.h`) 에 부재. 실제 IoMode = `kSimple=0` / `kAdvanced=1` / `kOfflineProcessing=2` 만 존재 — `kAdvancedKBypass` 는 plan 작성 시 fact-error. 본 구현은 `kAdvanced` 진입 시 bypass on, `kSimple` / `kOfflineProcessing` 시 bypass off 매핑으로 해석. test_vst3_bypass 7/7 PASS, RT 1000-iter alloc=0 통과. — Step 3 commit footer 잠금.
- [x] C2-Q18 (2026-05-09 M2 gate 결정): Step 4.1 의 strictness 게이트 = **B.2 (자체 in-process VST3 host fixture) 채택 — default 통과**. test_vst3_host_fixture 21/21 PASS + 6 추가 ctest (audio_smoke + param_layout 15 + param_roundtrip 30 + dispatch_rt_safety 1 + bypass 7 + state_persist 12) 모두 통과 — B.3 부채 회계 fallback 미발현. OFF dual-gate bytes/symbols MATCH (8593e347.../8ae874b5..., 1095e9e4.../3813547c... LC_ALL=C sort -u sha256sum). sysdep-free `ldd | grep -cE 'libX11|libfreetype|libasound|libwebkit'` = 0 + LD_DEBUG=libs 0 매치. vst3sdk SHA pin `1307ff43db9d2d4ac847c56847458da1ba2a7ee6cfe63b707f7f8e52ca977bb2` MATCH (inner JUCE repo `git ls-tree -r HEAD modules/juce_audio_processors/format_types/VST3_SDK | sha256sum`).
- [ ] C2-Q19 (Phase D6 deferral): Step 4.5 DAW confirm-only — Reaper or Bitwig 1종 이상에 plugin 로딩 + 6 param 동작 사용자 확인. **하드웨어 부재 환경 자동화 불가, 사용자 D+9 confirm 필요**. Cubase deferral = Phase D6 (plan §6 v3 AM-R3-7).

## spatial-engine-phaseC-C2B-postmortem - 2026-05-09

- [ ] C2B-Q1: `kBypass` parameter `defaultNormalizedValue` — 0.0 (off, Steinberg AGain convention) vs host-provided default? — Affects fresh-load behaviour in DAWs. Recommend stay 0.0 (matches `pluginterfaces/vst/ivsteditcontroller.h:71` example usage).
- [ ] C2B-Q2: When Phase D6 adds an 8th param, should the v2 reader accept ONLY v2 or ALSO v3 (forward-compat reader)? — Documents the version-ladder pattern. Recommend multi-version reader, single-version writer (always emit newest).
- [ ] C2B-Q3: `LD_DEBUG=libs` regex pattern — keep `'libX11|libfreetype|libasound|libwebkit'` (mirrors ldd-time gate at `.github/workflows/vst3.yml:55`) or widen to `'libGL|libdrm|libxcb'`? — Defensive widening may catch headless-vs-X11 driven regressions; defer until first GHA green.
- [ ] C2B-Q4: Strong-symbol `malloc` override — risk of conflict with future ASan run; should we add a CMake option `SPATIAL_ENGINE_RT_PROBE` (default ON) to switch off when ASan flagged? — Recommend yes, but only implement if/when ASan lane lands.
- [ ] C2B-Q5 (handoff): Post-sprint user-driven step F1 — DAW hands-on confirmation in Reaper + Bitwig + Studio One. Plug-in load, scan recognition, bypass button toggle, save/reload session preserves all 7 param values. Not autopilot-eligible.
- [ ] C2B-Q6 (Round-2 A9 deferral, 2026-05-09): If/when `core/src` introduces `realloc/aligned_alloc/posix_memalign`, the `vst3/tests/rt_alloc_probe.hpp` strong-symbol set must extend (currently only `malloc/free/calloc`). Trigger: explicit grep regression in CI OR RT-probe assertion firing on unexpected path. — Tracked in Risk R10. Not blocking C2B sprint.

## spatial-engine-phaseA-extended-M2-hoa-decoders - 2026-05-09

- [ ] M2HOA-Q1: AllRAD virtual-loudspeaker count default — `n_virtual = 5200` (3rd-order de-facto SPARTA/IEM) vs `1296` (smaller `.rodata`, slightly less smooth)? — Recommend 5200 with build-time knob `SPATIAL_ENGINE_ALLRAD_TDESIGN_SIZE` for embedded targets. Affects ~125 kB `.rodata` + OFF baseline `bytes.sha256` magnitude.
- [ ] M2HOA-Q2: max-rE per-order weight composition order — apply weights `g_l` to SH **before** pinv decode (Daniel 2000 canonical) vs **after** decode (cheaper, mathematically equivalent for orthonormal layouts only). — Recommend canonical pre-decode for correctness on irregular layouts.
- [ ] M2HOA-Q3: `DecoderType::PINV` enumerator value — pin to `0` so default-constructed and zero-initialised consumers map to existing behaviour. Document reservation of values 3, 4 for EPAD / in-phase in M2-v2.
- [ ] M2HOA-Q4 (cross-sprint coordination): Combined OFF baseline re-pin — must wait for both this sprint's HOA PR AND the C2B postmortem sprint's vst3/ PR to land before single re-pin commit. **DO NOT re-pin standalone.** On-call should verify C2B sprint state before merging this sprint's final commit.
- [ ] M2HOA-Q5 (deferred to next sprint): VST3 / WebGUI parameter exposure of `decoder_type` (Step S7 deferred). Owned by C2B-follow-on or a dedicated GUI sprint; not in this 1–2 wk ETA.
- [ ] M2HOA-Q6: AllRAD t-design table license — Hardin & Sloane public-domain coordinates assumed. **Verify** against IEM AllRADecoder source to confirm same table set is in use industry-wide; document attribution in `AllRADTDesigns.cpp` header.
- [ ] M2HOA-Q7: max-rE golden numerical reference — pin to SPARTA `getMaxREweights()` output OR IEM AllRADecoder's `MaxRE` weights? Both sources should agree to 1e-6; pick one as canonical and document delta in test fixture comments.
- [ ] M2HOA-Q8: `prepare()` rebuild latency budget — proposed AC `< 5 ms` for 16-speaker layout. Confirm acceptable for `OscDispatcher` control-thread context; if scenes change frequently in live theatre, may need `< 2 ms` and lazy per-order rebuild.

## spatial-engine-phaseA-extended-M2-hoa-decoders Round-2 amendment - 2026-05-10

User flipped Decision A: A-α (max-rE + AllRAD, 4/5) → A-γ (max-rE + AllRAD + EPAD + in-phase, 5/5). Following M2HOA-Q items added or updated to reflect 5-decoder reality.

- [ ] M2HOA-Q3 (Round-2 update): `DecoderType` enum now 5 values `{PINV=0, MAX_RE=1, ALLRAD=2, EPAD=3, IN_PHASE=4}` per S3. Reserve values 5–7 for future variants (EPAD2, mode-matching variants); B-β refactor trigger raised to ≥ 6 decoders (was ≥ 4 in Round-1).
- [ ] M2HOA-Q5 (Round-2 update): VST3 / WebGUI param exposure (S7) still deferred — but now needs to expose **5** enum values instead of Round-1's 3. Increases C2B-follow-on / GUI sprint scope marginally.
- [ ] **M2HOA-Q9 (Round-2 NEW): EPAD weights paper citation precision.** Multiple EPAD formulations exist in literature: (a) Zotter & Frank 2012 ICSA "All-Round Ambisonic Panning and Decoding" (energy-preserving via SVD on E^T E); (b) Heller et al. 2014 AES "Is My Decoder Ambisonic?" (alternate normalisation). SPARTA implements (a) per Politis 2018 thesis. Pick (a) as canonical for AC-S2.5.3 golden vector; document alternative formulations in `EPADDecoder.cpp` header comment so future maintainers don't accidentally swap normalisation.
- [ ] **M2HOA-Q10 (Round-2 NEW): Decoder N>3 parity gap vs Spat Revolution N=7.** Engine `MAX_ORDER = 3` (`core/src/ambi/AmbiDecoder.h:18`); Spat Revolution / SPARTA / IEM all support up to N=7 (64 SH channels). Round-2 plan delivers **decoder-count parity (5/5)** but does NOT close the **order parity** gap (N=3 vs N=7). Affects all 5 decoders equally (encoder + each decoder must extend). Surface area: encoder ~200 LOC for orders 4–7 (Y_l^m formulas), decoders need per-order weight tables extended, RT cost grows quadratically (S × K = 24 × 64 = 1536 mul/sample for 7th-order on 24-spk vs current 24 × 16 = 384). Recommend: separate "MAX_ORDER bump" plan with own ADR — independent of this Round-2 sprint, scheduled post-completion.
- [ ] **M2HOA-Q11 (Round-2 NEW): EPAD SVD implementation choice.** Options: (a) reuse existing Gauss-Jordan pattern from `core/src/ambi/AmbiDecoder.cpp:34-62` extended to Jacobi eigendecomposition on `E^T E` (no external dep, ~80 LOC, single-precision intermediate may lose conditioning); (b) link Eigen3 (header-only, robust SVD, but adds ~3 MB header dep + OFF baseline binary impact); (c) link OpenBLAS LAPACK (~20 MB runtime dep, pulled-in only on Linux). Recommend (a) for OFF baseline minimisation + zero new dep; document precision concern in `EPADDecoder.cpp` header.
- [ ] **M2HOA-Q12 (Round-2 NEW): Per-decoder OSC verb granularity.** Round-2 ships `/spe/ambi/decoder_type i {0..4}` (single global selector). Spat Revolution allows **per-bus decoder selection** (one bus uses max-rE, another EPAD). Out of scope this sprint; flag for Phase B/C planning. If multi-bus selection becomes a P0 user request, the OSC verb pattern needs `/spe/ambi/<bus_id>/decoder_type i N` and `AmbiDecoder` would need to be instantiated per-bus.
- [ ] **M2HOA-Q13 (Round-2 NEW): EPAD vs AllRAD on irregular layouts — overlap or complement?** Both decoders address irregular speaker layouts but via different mechanisms (AllRAD = virtual-loudspeaker VBAP composition; EPAD = singular-value energy normalisation). On the same irregular hemi-dome (AC-S2.2 + AC-S2.5.2 both touch this), **measure and document** which decoder produces lower energy variance — informs default decoder recommendation in user docs. Not a sprint blocker; output is a documentation note + benchmark CSV in `core/tests/core_unit/`.

## spatial-engine-phaseA-extended-M2-hoa-decoders Round-2 final (Critic R1 fixes) - 2026-05-10

Round-2 Critic R1 review applied 4 CRITICAL + 4 MAJOR + 2 MINOR fixes. M2HOA-Q items below updated/added/closed accordingly.

- [x] **M2HOA-Q4 (CLOSED, Round-2 Critic A10):** Combined OFF baseline re-pin coordination obsolete. C2B postmortem sprint merged at `acb8c27` (2026-05-09); OFF baseline re-pinned at `ec2510d`. This sprint's S6 produces a single solo re-pin commit. No cross-sprint blocking required.
- [ ] **M2HOA-Q3 (Round-2 final update, Critic A8 contradiction-fix):** `DecoderType` enum has 5 explicit values `{PINV=0, MAX_RE=1, ALLRAD=2, EPAD=3, IN_PHASE=4}`. **Drop the "reserve values 5-7" pre-allocation language** (was in Round-2 initial). Enum grows naturally as needed; ABI is stable as long as enumerator values are explicit. The B-β refactor trigger at decoder count ≥ 6 is **structural switch-dispatch fragility**, not ABI breakage.
- [x] **M2HOA-Q2 (RESOLVED 2026-05-10):** SH-side pre-multiply chosen — canonical SPARTA/IEM/Daniel 2000 convention. At `prepare()` time: `D_combined[s,k] = g_{floor(sqrt(k))}(N) * D_pinv[s,k]` baked into S×K matrix. RT decode path (`AmbiDecoder::decode()`) unchanged — reads same matrix, zero branch. Implemented in MaxREDecoder and InPhaseDecoder compose helpers. Storage shape preserved. Commit footer: `M2HOA-Q2: SH-side pre-multiply, canonical SPARTA/IEM (Appendix B1)`.
- [x] **M2HOA-Q7 (RESOLVED 2026-05-10):** Locked golden vectors via Legendre-root formula `g_l = P_l(r_E_max)` (Zotter & Frank 2019 eq.4.49, SPARTA canonical). N=1 root of P_2: x=1/sqrt(3)=0.57735; N=2 root of P_3: x=sqrt(3/5)=0.77460; N=3 root of P_4: x≈0.86114. Golden vectors (tol 1e-5): N=1={1.0000,0.5774}, N=2={1.0000,0.7746,0.4000}, N=3={1.0000,0.8611,0.6124,0.3045}. Round-1 g_1≈0.7071 was WRONG (cos(45°), not Legendre root). AC-S1.0 CLOSED. Commit footer: `M2HOA-Q7: Legendre-root canonical, SPARTA-verified golden vectors`.
- [ ] **M2HOA-Q11 (Round-2 final update, Critic A7):** EPAD SVD implementation = **Jacobi-on-(E·E^T or E^T·E) — pick the smaller well-conditioned form**: `E·E^T` (S × S) when `S ≤ K`, else `E^T·E` (K × K). Implements from scratch (~80 LOC); `solveSPD()` reuse from `AmbiDecoder.cpp:34-62` is **invalid** (Gauss-Jordan inverse, not eigendecomposition). Tikhonov regularisation pattern from `:114-120` IS reused as the EPAD fallback when convergence fails OR condition number > 1e10. Sweep cap = 100; tolerance = `1e-12 · trace(A)`.
- [ ] M2HOA-Q5 (unchanged Round-2): VST3 / WebGUI param exposure (S7) still deferred. Now exposes 5 enum values (was 3 in Round-1).
- [ ] M2HOA-Q9 (unchanged): EPAD weights paper citation — pin SPARTA `getEPADdecoder()` / Politis 2018 thesis as canonical for AC-S2.5.3.
- [ ] M2HOA-Q10 (unchanged): Decoder N>3 parity gap vs Spat Rev N=7 — separate "MAX_ORDER bump" plan deferred. Round-2 Critic A9 reframe: 5/5 score is **algorithm-count parity at N=3**, not full parity.
- [ ] M2HOA-Q12 (unchanged): per-bus decoder selection (Spat per-bus pattern) deferred to Phase B/C.
- [ ] M2HOA-Q13 (unchanged): EPAD vs AllRAD irregular-layout overlap — measurement + benchmark CSV.
- [x] **M2HOA-Q14 — RESOLVED 2026-05-28 by v0.8 audit P1.1 (commit `64352df`).** Decision: invoke via `SpatialEngine::applyPendingAmbiDecoderChange()` from a new ~1 Hz control tick in `core/src/bin/spatial_engine_core.cpp:592-660`, behind a lock-free double-buffer in `AmbiDecoder::decode_matrices_` (2 slots × MAX_ORDER, `std::atomic<int> active_slot_` publishing). The naive "just call from a tick" approach was a UAF (Critic C2) because the original `decode_matrices_` was non-atomic single-buffered; the published design separates rebuild (control thread, allocating) from read (audio thread, load-acquire once per block). Concurrency test `test_p_ambi_decoder_double_buffer_race` (relacy + std::thread stress) drives rapid back-to-back switches faster than the 1 Hz production cadence so the slack invariant cannot be silently regressed.
- [ ] **M2HOA-Q15 (Round-2 NEW, Critic Appendix B3):** RT-safe guard comment block at `AmbiDecoder.cpp:142` decode entry — confirm wording with reviewer before commit. Block prevents future engineers from adding `switch (type_)` inside `decode()`. Treated as load-bearing documentation per Principle 2.

## spatial-engine-phaseC4-and-v0.2-release - 2026-05-10

- [ ] **C4-Q1 (Decision A1 cert evidence):** Concrete proof that A1-α (per-instance auto-port UDP bind in plugin process) would fail on Pro Tools / Logic / Ableton, vs projection? Need a specific Steinberg/Avid/JUCE forum thread or Apple sandbox doc citation to ratify A1-δ over A1-α. — Affects whether A1-δ sidecar architecture is justified vs simpler A1-ε recv-only socket. If evidence is weak, revisit A1-ε as primary.
- [ ] **C4-Q2 (Decision A2.1 state v3 timing):** State format bump v2 → v3 (4 → 8 params) for `kMute` separation — land in v0.2 with C3 only (B1-β, OFF re-pin twice) OR defer to v0.3 alongside C4? Wasted bump if v0.2 is C3-only. Recommend defer to v0.3. — Affects R2 OFF re-pin scope and `vst3/tests/test_vst3_state_persist.cpp` extension timing.
- [ ] **C4-Q3 (Decision B1 final scope):** B1-β (v0.2 = C3-only, recommended) vs B1-α (v0.2 blocks on C4) vs B1-γ (v0.1.1 patch + v0.2 with C4)? Architect must pick. Recommended path keeps Track A and B independent; alternative couples release to highest-risk work. — Affects entire ETA banner.
- [ ] **C4-Q4 (R3/S8 DAW choice scope):** Reaper + Bitwig (recommended, both Linux-friendly) vs add Ardour or REAPER ARM for broader coverage? — Affects v0.2 hardening cost and first-customer regression catch rate. Recommend stay at 2 hosts for v0.2; expand at v0.3.
- [ ] **C4-Q5 (Sidecar IPC primitive):** UNIX domain socket + JSON registry (recommended for debuggability) vs shared memory (`shm_open` + SPSC ring) for lower latency? — Affects sidecar S3/S4 implementation; shm path is more complex but eliminates one syscall per param-change. Recommend domain socket for v0.3 ship; shm as future optimisation.
- [ ] **C4-Q6 (ADR 0007 vs 0008 split):** One combined ADR (binding model + discovery) or two separate as drafted (0007 binding + 0008 discovery)? Combined is shorter; separate keeps each decision auditable. Recommend separate. — Affects S1 deliverable count.
- [ ] **C4-Q7 (`IConnectionPoint::notify` reversal):** AM-R3-10 set `notify=kNotImplemented`; C4 S4 reverses this. Any back-compat risk with hosts that previously relied on `kNotImplemented`? Most hosts treat `kNotImplemented` as no-op, so reversing to a real handler should be transparent. Confirm with Architect citing VST3 SDK convention. — Affects controller-side test updates.
- [ ] **C4-Q8 (B3-β GHA prebuilt artifact target):** ubuntu-24.04 GLIBC 2.39 (current vst3.yml host) only, OR add ubuntu-22.04 for older glibc systems? Affects user reach but doubles CI cost. Recommend stay 24.04 for v0.2; matrix expands at v0.3. — Affects R5 GHA workflow.
- [ ] **C4-Q9 (Pre-mortem 3 escape threshold):** "Any R3 step that fails 2 retries triggers escape to v0.1.1" — is "2 retries" the right threshold or should it be 1 (more conservative) or 3 (more permissive)? — Affects how quickly we abandon v0.2 in favour of v0.1.1 if Reaper has unexpected issues.
- [ ] **C4-Q10 (Manual locale strategy for plugin):** S5 Manual Ch.5 in Korean only (matches existing manual locale) vs add English appendix? Recommend KR-only matching `docs/manual_kr/` scope; English equivalent comes with v0.3 international expansion. — Affects S5 effort.

## spatial-engine-webgui-v1 - 2026-05-12 (Round-2 RALPLAN-DR)

- [x] **WGUI-Q1 (CLOSED Round-2, Architect ACCEPT)**: `/scene/list` reply 양방향 통신 — 본 sprint scope 외, ADR-2 deferred. 현재 `core/src/ipc/SceneController.cpp:26-28` 메모리만 저장, `core/src/ipc/CommandDecoder.cpp:371-373` decode-only. 별도 plan 큐로 이관 (신규 OSC dialect + JUCE 메시지-스레드 응답 라우팅 + osc_bridge 9101 수신 확장 필요). G5는 송신 정합만 검증.
- [x] **WGUI-Q2 (CLOSED S0, ADR-0014 commit 6d90ffb)**: `ui/tests/` 76 vs 63 archaeology 완료. 결과: **zero deletions ever** — `git log --all --diff-filter=D --summary -- ui/tests/` 결과 0건, plan의 "76"은 R0/R1 estimation artefact. G6 acceptance N=63 frozen. Pre-mortem §5-E (deleted test 회귀) discharged.
- [x] **WGUI-Q3 (CLOSED Round-2, Architect ACCEPT-WITH-MOD + Critic N2)**: 60fps methodology — desktop 60 / mobile 50 / **min-of-5-windows-p10** / N=100 sample (5 windows × 5s @ 250ms sampling) / 64-obj concurrent drag synth (dev-only `?seed=64obj` query). R1의 N=20은 통계적 무의미 평가, R2는 N≥100 또는 min-of-windows 표준 채택.
- [x] **WGUI-Q4 (CLOSED Round-2)**: 매뉴얼 챕터 번호 — Ch.6 (`docs/manual_kr/CH6_WEBGUI.md`), Ch.5는 VST3 + ADM-OSC 직결 모드 (기존 commit `a492ef9`).
- [x] **WGUI-Q5 (CLOSED Round-2)**: 실기 smoke 게이트 강도 — emulation (iPhone 13 + Pixel 5 playwright)으로 본 sprint G2/G4 게이트 충족 가능. 실제 단말 G8은 conditional / out-of-scope (Phase 0 MOU 필요).
- [x] **WGUI-Q6 (CLOSED S4, commit 2de8e93)**: G4 ±2° 좌표 정밀도 — S4 playwright desktop emulation 실측 결과 **azim diff = 0.0946°** (보수 상한 2° 대비 21배 여유). 2° 상한 유지 (relax 불필요). derivation: 32-bit float wire + canvas 1px ≈ 0.5° @ 720px → 측정값이 이론치 안에 들어옴.

## spatial-engine-v0.5.1-binaural-hotfix - 2026-05-16

- [x] **V051-OQ1 (CLOSED v0.7 plan §0.3 Decision B):** `/sys/binaural_status` stays at 1 Hz (already implemented via D-M4 sub-tick counter at `vst3/SpatialEngineProcessor.cpp:1258`). Diagnostic telemetry (`/sys/binaural_diag`) is **event-driven** (on demote latch), NOT 1 Hz — chosen over 1 Hz polling and 32-block pre-demote window to minimize log noise + storage burden. v0.7 D-S3 may re-evaluate to a pre-demote-window summary (B-3) in v0.8 after real telemetry is observed.
- [x] **V051-OQ2 (CLOSED v0.6 ship):** Runtime sticky-underrun auto-demote landed as committed v0.6 #5 (not stretch). 8-strike × 90% threshold ships; v0.7 D-S2 makes the strike count block-size-aware (`max(8, ceil(0.020s/block_seconds))`).
- [ ] **V051-OQ3 (Q3 documentation surface):** SOFA-missing fallback note — `docs/USER_GUIDE.md` (if it exists) vs v0.5.1 release notes only vs both. Affects integrator-facing communication surface for the silence + warning behaviour change.

## spatial-engine-v0.7 - 2026-05-18 (Planner pass)

- [ ] **V07-Q1 (D-S3 telemetry cadence trade-off, decision B in plan §0.3):** Event-driven `/sys/binaural_diag` on demote latch (B-1, selected) vs continuous 1 Hz (B-2) vs pre-demote-window summary (B-3, deferred to v0.8). v0.7 ships B-1; re-evaluate after ≥ 1 cycle of telemetry data. — Why it matters: B-1 misses slow-degradation patterns; B-3 needs telemetry-volume reality check before designing.
- [ ] **V07-Q2 (D-S1 cooldown duration):** `kResetDemoteCooldownNs = 60s` chosen as default — too short (user ping-pongs and gets glitches) or too long (user can't recover from one-off spike quickly)? Architect to confirm or override. — Why it matters: First operational UX cycle for runtime demote recovery.
- [ ] **V07-Q3 (D-S4 relacy false-positive contingency, pre-mortem §7.1 Scenario B):** If relacy reports false positives on the v0.6 #9 fix, drop relacy for `cppmem` or hand-rolled `std::thread` stress? Plan default: drop relacy after one cycle of unstable PASS direction. — Why it matters: Choosing the synthetic-verification tool now affects every future weak-memory-order fix's defensibility.
- [ ] **V07-Q4 (D-S5 Linux ARM64 pre-promotion soak length):** 5 consecutive green triggers (plan §7.1 Scenario C mitigation) vs 1 vs 10? — Why it matters: Affects merge velocity during the v0.7 transition; balances confidence vs sprint length.
- [ ] **V07-Q5 (Item #6 legal-review trigger ownership):** ADR 0016 Limitations & legal review status section names "trigger events" but does not name an OWNER for each trigger. Should the project lead be the default trigger-owner, or should each band have a designated reviewer? — Why it matters: Without an owner, triggers fire and nothing happens.
- [ ] **V07-Q6 (Item #4 relacy vendoring vs submodule):** Pin relacy via git submodule (canonical upstream, easier diff-tracking) vs vendor a copy in `third_party/relacy/` (zero network dependency at clone time)? Plan default: vendor with explicit upstream commit hash + LICENSE-relacy header. — Why it matters: First external dev-dep choice; sets precedent for future verification tools (cppmem, etc.).
- [ ] **V07-Q7 (Item #7 P-tag CI gating strength):** `scripts/audit_ptags.py --strict` as a blocking pre-commit hook vs CI-only soft warning vs documentation-only? Plan default: documentation-only for v0.7 (too eager for in-progress weekly reports). — Why it matters: P-tag chain integrity vs in-progress doc-writing friction.
- [ ] **V07-Q8 (Item #3 D-S3 audio-thread CAS-vs-relaxed trade-off, pre-mortem §7.1 Scenario A):** Max-ratio update uses CAS for precision but adds atomic-contention cost. Fallback to `store(max(...), relaxed)` (last-writer-wins, ~1 strike of precision loss)? Plan default: keep CAS until benchmark proves > 50 ns/call median. — Why it matters: Self-fulfilling-demote-prophecy risk in v0.6 #5 redux.

### Deferred to v0.7.x (decided 2026-05-21 at §8 finalize)

- [ ] **V07-DEFER-1 (`bench_heartbeat_drain_latency` ctest, Critic §C.1):** The heartbeat-drain wall-clock perf-guard ctest (`+50%` baseline fail) promised by the plan as the 3rd new ctest was NOT implemented in the v0.7 cycle. Deferred to v0.7.x. The D-S3 drain extension (`/sys/binaural_diag` emit) is functionally tested via `test_binaural_diag_emitted_on_demote` but has no automated regression gate on its drain cost. — Why it matters: a future D-S3-adjacent change could silently regress heartbeat drain latency without a guard. v0.7.x should implement it (capture median over 50 samples, persist baseline to `core/tests/core_unit/heartbeat_drain_baseline.txt`).

## spatial-engine-adr0019-pr2-shared-ring-backend - 2026-05-22 (Planner / RALPLAN-DR DELIBERATE)

- [ ] **PR2-Q1 (Decision 1 — warning observable):** PR2 ships named diagnostic counters (`staleWarningCount`/`pacingWarningCount`/`underrunWarningCount`/`attachedNoDataWarningCount`) as the cases-5/6 observable, NOT a `DiagnosticSink` observer. Confirm with Architect/Critic that PR4 wiring `/sys/warning shm_*` OSC on these counter edges is acceptable as additive (vs. PR2 pre-defining the warning-code enum that PR4 owns). — Why it matters: locks the seam between PR2 (detection+rate-limit policy) and PR4 (transport); a wrong call here forces PR4 rework.
- [x] **PR2-Q2 (CLOSED iter-2 M2 — case 8 enforcement under RT_ASSERTS=OFF):** `build_off` has `SPATIAL_ENGINE_RT_ASSERTS=OFF`. **Honest accounting:** the recording `operator new` override TU is `#if SPE_RT_ASSERTS`-compiled-out (`RtAssertNoAlloc.cpp:8`), so `RtAssertNoAllocScope` only bumps a per-thread depth counter nothing reads — `rt_alloc_violations()` is INERT/vacuously 0 there. The earlier "self-arm regardless of flag" claim was wrong and is removed. **Resolution:** in `build_off` the only meaningful alloc guard is the buffer pointer/capacity-stability invariant (case 8 invariant iv); the `rt_alloc_violations()==0` sentinel target is registered behind `if(SPATIAL_ENGINE_RT_ASSERTS)` (mirror `core/tests/core_unit/CMakeLists.txt:106`) and exercised in a dedicated `-DSPATIAL_ENGINE_RT_ASSERTS=ON` ctest run **in PR2 (not PR7)** as a registered, reproducible target.
- [x] **PR2-Q3 (CLOSED iter-2 — no-syscall enforcement):** No syscall sentinel exists in the tree. Accepted for PR2: the no-syscall half is enforced by (1) `pump_block(cb, hw_ts_ns)` taking the timestamp as a param so its body contains zero clock reads (iter-2 S1) and (2) a documented review gate (body = loads/stores/memcpy/memset only). A `seccomp`/`strace` probe is explicitly out of PR2 scope; revisit if a syscall regression is ever observed. — Why it matters: a hidden syscall on the RT path would pass all PR2 tests; the param-not-call design removes the most likely source (clock reads).
- [x] **PR2-Q4 (CLOSED iter-2 M1 — case 7 pow2 semantics):** Consumer-side round-up is **REJECTED as memory-unsafe** — rounding masking capacity to 8192 against a producer that mapped `total_region_bytes(channels, 6000)` indexes past the producer's mapping → cross-process out-of-bounds read / SIGSEGV. Masking capacity is ALWAYS `header.capacity_frames`. Non-pow2 is rejected at `start()` (`BackendError::DeviceOpenFailed`); case renamed `ring_capacity_non_pow2_rejected_on_attach`. ADR §2.4 "padded to 8192 … logs once" is a **producer-side** obligation, OUT of PR2 scope. — Why it matters: this was the SIGSEGV-class bug the Architect caught; the wire layout is producer-owned and the consumer must never assume a larger capacity than mapped.
- [ ] **PR2-Q5 (test accessors):** Case 8 (buffer-pointer stability) and case 7 (effective masking capacity) need test-only introspection. Decide between minimal public test accessors (mirroring `NullBackend::set_input_fixture`/`pump_synchronous`) vs `friend`-test. Plan leans public test accessors. — Why it matters: sets the test-hook precedent for PR3 (cases 9–10) on the same class.
- [x] **PR2-Q6 (CLOSED iter-2 C2 — attach()/start() contract):** `attach()` returns `std::unique_ptr<SharedRingBackend>` (per ADR §3.1) and CANNOT return `BackendError`. Resolved toward Critic option (b): `attach()` does mmap only (non-null on success, no header validation); ALL header validation (magic/version, divisor, `block_size>512`, `block_size<=capacity_frames`, pow2) moves to `start()` which returns `BackendError` (matches `NullBackend::start` gate `NullBackend.cpp:73-75`). Cases 1/2/7 assert `start()`'s return, not `attach()`'s. — Why it matters: the original plan asserted an impossible contract (Critic CRITICAL C2).
- [x] **PR2-Q7 (CLOSED iter-2 S2/M-A — distinct error code):** Divisor / `block_size>capacity_frames` mismatch gets new `BackendError::BlockConfigMismatch` (`describe()` → `"block_config_mismatch"`), added to the exhaustive switch in `AudioBackend.cpp` (no `default:`; omitting the case = `-Werror=switch` break). `BlockSizeExceedsMax` stays strictly for `header.block_size > MAX_BLOCK(512)`. Engine block passed to `start()` is ≤512 (concretely 256) in every case so case 2's divisor sub-case exercises the divisor gate, not the MAX gate. — Why it matters: overloading `BlockSizeExceedsMax` for two distinct failures made case 2 ambiguous (Critic M-A).

## spatial-engine-adr0019-pr3-engine-wiring - 2026-05-24 (Planner / RALPLAN-DR DELIBERATE, iter-2)

- [ ] **PR3-Q1 (Decision 1 — resync trigger boundary; iter-2 C1 corrected to STRICT `>`):** Case-9 resync uses a gap-gated clamp: resync `read_idx_local_ = write_idx - block` (store-release it to `header.read_idx` as the PR5 handshake) ONLY when `backlog (= write_idx - read_idx) > capacity_frames` **STRICTLY**; verbatim seed `read_idx_local_ = hdr_ri` otherwise (incl. the `== C` resident boundary). Derivation: `hdr_ri` overwritten ⟺ `hdr_ri < write_idx - capacity` ⟺ `backlog > C`; at `== C`, `hdr_ri` is the oldest STILL-RESIDENT frame → play it, don't skip. Both iter-1 `>= C` and the Architect-suggested `> C - block` were wrong; `> C` is exact (resync is single-threaded in `prepare()` before the worker spawns — no concurrency margin needed). Confirm with Architect this STRICT-`>` reading is the intended case-9 semantics vs "always resync on attach"; fallback = option (a) `AttachMode::ResyncToHead` with cases 3/4 opting into verbatim. — Why it matters: the only place the §7.1 title and the case-4 contract (`test_shared_ring_backend.cpp:402-442`, b0-first) can read as conflicting; the `== C` trap regresses a resident block if mis-gated.
- [ ] **PR3-Q2 (Decision 2 — consumer-lock offset):** `kConsumerLockOffset = 0x0058` (first `_reserved` byte, 8-aligned) holds the consumer attach-lock `atomic<uint32_t>` (CAS 0→getpid in start() Gate 7; `kill(pid,0)`-ESRCH stale reclaim; CAS release in stop()). Confirm no future v2 header field is informally promised that slot; PR4's `/sys/state` consumer-alive could share this word. Wire stays version=1 (producer zero-inits `_reserved`, PR5-compatible). — Why it matters: the offset is load-bearing for PR5 (Python producer) + PR7 (Windows `OpenProcess` liveness); changing it later is a wire break.
- [ ] **PR3-Q3 (Decision 3 — output channel count source):** CLI passes `--channels` (speaker count) as the backend's `output_channels` for engine render staging. Engine clamps `min(output_channel_count, n_spk)` (`SpatialEngine.cpp:703`, M5 anchor fix) so over-count is safe, under-count truncates. Confirm `--channels` equals the engine speaker-layout channel count in all configs. — Why it matters: a mismatch silently truncates output channels.
- [ ] **PR3-Q4 (diagnostics tick deferred to PR4):** PR3 does NOT call `poll_diagnostics` from the standalone loop (`spatial_engine_core.cpp:499-537` has no control tick); cases 9/10 drive `poll_diagnostics` directly in-test. Confirm Architect accepts deferring diagnostics-tick wiring to PR4 (where real `/sys/warning` emit lands). — Why it matters: a live shm run emits no stale/pacing warnings until PR4; acceptable per ADR §8 PR split but worth ratifying.
- [ ] **PR3-Q5 (test-hook precedent, inherited from PR2-Q5):** PR3 reuses the public-test-accessor pattern (`stagingData()`/`stagingCapacity()`/`maskingCapacityFrames()`) for cases 9/10 introspection (e.g. `consumer_lock_atomic` access from the test, output-staging `.data()`/`.capacity()`). PR2-Q5 deferred the cases-9/10 test-hook precedent to PR3; resolved here toward minimal public test accessors (no `friend`), consistent with PR2. — Why it matters: locks the test-hook style for the same class across PR2→PR3.
- [x] **PR3-Q6 (Decision 3(i) deviation — worker spawned ONLY on the 3-arg `start(cb,eb,out)`; RATIFIED at implementation, blessed by Phase-4 Architect + code-reviewer):** The plan's Decision-3(i)/checklist-step-4 prose said "spawn `worker_` at the end of `start()`," but every frozen PR2 SharedRingBackend case drives the **2-arg `start(&cb, kEngineBlock)` + manual `pump_block`** (canonical: case 4 at `test_shared_ring_backend.cpp:418` then manual pumps at `:430/438/446/456`; same shape at `:332/336/357`, `:381/391`, `:517/526`, `:541/551/555`, SEC-1b `:582/586`, structural `:916/919/929`). A worker spawned by the 2-arg form races those manual pumps on `read_idx_local_`/`xruns_`/capture (observed: 5/5 deterministic abort at case 4). Resolution: a private `start(cb, eb, out, bool spawn_worker)`; ONLY the 3-arg public `start(cb,eb,out)` (the CLI path `spatial_engine_core.cpp:524` + AC-5) sets `spawn_worker=true`; 1-/2-arg pass `false` but STILL `acquire_consumer_lock=true` (Gap-b intact). This honors the plan's OWN invariants (P1 "test path stays thread-free"; AC-5 "the ONLY AC that uses the real worker thread") over its loose wording — the plan prose was the defect, not the code. Architect verdict: blessed (superior to literal plan text); code-reviewer: blessed (correct, minimal, documented inline at `SharedRingBackend.cpp` deviation note + `.h`). — Why it matters: a future reader must not "fix" the 2-arg form to spawn a worker (it would re-introduce the race); the plan's Decision-3(i) wording is corrected to "the 3-arg `start()`".
- [ ] **PR3-Q7 (deferred hardening from Phase-4 security review — NON-BLOCKING, Risk LOW):** Two defense-in-depth notes the security-reviewer carried (verdict APPROVE-WITH-NOTES; neither reachable as an exploit inside ADR §10's same-host, 0600-file-perm, unauthenticated trust boundary): (a) **RAII lock-guard in `start()`** — if `callback_->prepareToPlay()` (not `noexcept`, `AudioCallback.h:43`) or the `std::thread` ctor (EAGAIN under thread-exhaustion) throws AFTER Gate 7 acquired the consumer lock, the lock is held until `~SharedRingBackend()→stop()` releases it (no PERMANENT leak, but a wider window than necessary on a long-lived object). A scope-guard releasing on any non-Ok `start()` exit would free it at the throw site. (b) **`--input-backend shm:<path>` regular-file branch** (`SharedMemoryRegion.cpp:104-120`) opens an arbitrary operator-supplied path with `O_RDWR` and follows symlinks (no `O_NOFOLLOW`); content is never disclosed (mapping is gated by the magic check + never printed) and write-back only after all gates pass — explicitly inside the same-host operator trust boundary. Verified SAFE: the sole `kill(expected, 0)` uses a hardcoded `sig=0` and is never escalated; `kill(-1,0)`/`kill(0,0)` deliver no signal and the `==-1 && ESRCH` reclaim predicate fails-safe (refuse) on adversarial lock values. — Why it matters: tracked so PR4/PR7 can fold the RAII guard + an ADR §10 symlink note; not a PR3 blocker. **(PR4 update: D5 DEFERS this to PR7 — see PR4-Q4.)**

## spatial-engine-adr0019-pr4-telemetry - 2026-05-25 (Planner / RALPLAN-DR DELIBERATE, iter-2 — Architect APPROVE-WITH-CONDITIONS + Critic ITERATE applied: CR-1/CR-2 + MJ-1..4 + fold-ins)

- [ ] **PR4-Q1 (Decision 2 — clock-domain contract, BLOCKING-FOR-PR5):** PR4 feeds `now_ms = now_unix_ms` (system_clock wall-clock ms) to `poll_diagnostics`, per ADR §2.3 line 81 ("`producer_heartbeat_ms` = wall-clock ms"). For cross-process stale detection to be correct, **PR5's `ipc_sink.py` producer MUST write `producer_heartbeat_ms` as unix-epoch ms** (e.g. `int(time.time()*1000)`), NOT a steady/monotonic clock. Confirm with Architect this is the intended contract (vs. a steady-clock heartbeat that would require the consumer to track a clock offset). — Why it matters: a steady-ms producer heartbeat against a wall-clock-ms consumer `now_ms` is off by the unix epoch (~1.7e12 ms) → stale always fires; the bug is invisible same-process (PR4 ctests inject the clock) and only surfaces in the PR6 soak.
- [ ] **PR4-Q2 (Decision 1 — `shm_underrun` detail-string content):** PR4 emits the cumulative `xrunCount()` total as the `shm_underrun` `,iis` detail (coalescing multiple RT increments per tick). Confirm the soak/operator wants the running total vs. the per-tick delta vs. the ADR §2.5 `"<channels=…>"` style. The D-5 precedent sent a meaningful scalar (age-seconds); the running total is the analogous choice. — Why it matters: a downstream `/sys/warning` consumer (WebGUI / soak) parses this detail; the shape is a soft contract PR6 asserts.
- [ ] **PR4-Q3 (Decision 3 — `shm_consumer_locked` value semantics):** PR4 reports boolean 0/1 (the field is `_locked` by name; the holder is always this engine's own PID once running). Confirm no operator use-case wants the holder PID exposed (e.g. to diagnose a stale cross-process lock). If yes, a separate `shm_consumer_lock_pid` key (additive) is the place, not overloading `_locked`. — Why it matters: PID-on-wire is a privacy/diagnostic trade-off; locking the boolean now keeps the wire minimal.
- [ ] **PR4-Q4 (Decision 5 — PR3-Q7 hardening deferral RATIFICATION):** PR4 DEFERS the PR3-Q7 RAII consumer-lock-guard + `--input-backend shm:<path>` `O_NOFOLLOW` note to PR7 (orthogonal to telemetry, Risk-LOW, non-exploitable inside ADR §10's trust boundary; PR7 already reworks consumer-lock liveness + `SharedMemoryRegion`). Confirm Architect accepts the deferral (vs. folding the one-line graft into PR4 and re-validating the AC-3/AC-4 consumer-lock ctests). — Why it matters: folding it in re-touches frozen PR3 `start()`/Gate-7 and mixes a security-hardening change class into a pure-telemetry PR.
- [ ] **PR4-Q5 (Decision 4 — tick cadence; MJ-1 reworded):** PR4 ticks `poll_diagnostics` + the emitter at 1 Hz (shm-gated), matching the D-5 watchdog rationale (the stale/pacing/no-data source rate-limits dominate; ADR §6 "10 Hz" is a ceiling, not a requirement). Confirm 1 Hz is acceptable for the stale-detection latency budget (≤ 1 s to first warning, then 1/30 s rate-limited). **A future cadence bump is SAFE for `shm_underrun` because the emitter owns its 1/s guarantee via the `last_underrun_emit_ms_` wall-clock latch (MJ-1), NOT via the tick rate** — so raising the tick to 10 Hz tightens first-detection latency without breaking the ADR §6 "shm_underrun (1/s)" contract. — Why it matters: trades first-warning latency vs. per-tick `system_clock`/header reads; the emitter-owned underrun latch decouples the 1/s contract from the cadence so a bump cannot silently regress it.
- [ ] **PR4-Q6 (Decision 3 / MJ-2 — fast `producer_state` transition 1 Hz-coalesced; NEW iter-2):** the emitter reads `producer_state()` once per 1 Hz tick (D4), so a `Draining(2) → Closed(3)` transition shorter than the tick period is coalesced and `shm_producer_state=2` may never emit, though ADR §6 (`:225-226`) treats Draining and Closed as distinct operator states. Resolve: EITHER (a) PR5's producer holds each non-terminal state ≥ one PR4 tick period (a producer obligation, like PR4-Q1) so the consumer observes every transition, OR (b) PR6's soak asserts only the TERMINAL `Closed` state cross-process and treats intermediate-state observation (`Draining`) as best-effort. PR4 itself adds no sub-tick state sampling (no producer→consumer state-change signal exists on the wire — out of PR4 scope). — Why it matters: an operator/soak relying on observing `Draining` distinctly from `Closed` needs the producer-side hold (a); without it the 1 Hz tick can skip the intermediate state. **(PR5 update: D2 DISCHARGES this via option (a) — `drain_dwell_s=1.0` default holds `Draining` ≥ 1 engine tick; see PR5-Q2.)**

## spatial-engine-adr0019-pr5-adm-player-sidecar - 2026-05-25 (Planner / RALPLAN-DR DELIBERATE, iter-2 [2026-05-27] — Python producer; discharges PR4-Q1 + PR4-Q6)

> **iter-2 review status (2026-05-27):** Architect = APPROVE-WITH-CONDITIONS (3); Critic = ITERATE (2 CRITICAL + 3 MAJOR). Both CRITICALs were verified on this host (Python 3.12.3): [C1] AC-9 vacuous under the GIL (re-scoped to program-order only; memory-ordering → PR6); [C2] `IpcRingSink.close()` `BufferError` from live `np.frombuffer(shm.buf)` views (fixed: drop views + `gc.collect()` before close; new AC-13). MAJORs applied: AC-1 pins all 15 offsets incl. unaligned atomics; "clean tree" claim corrected (dreamscape VOLATILE); drop-newest naming unified; `sched_yield` reframed (not a fence); seam bypasses device logic (headless AC-11). All five PR5-Q below were RATIFIED by both reviewers (topology direction confirmed; only the Q5 "clean tree" sub-claim was false → corrected). Plan iter-2 applied; pending a focused Critic re-verify for APPROVE consensus before autopilot.

- [ ] **PR5-Q1 (Decision 1 — round-trip test strategy / cross-language proof ownership):** PR5's pytest is a PURE-PYTHON round-trip — the test writes via `IpcRingSink` and reads the ring back with an in-test reader mirroring the C++ consumer's masked-index read (ADR §2.4), with AC-1 pinning literal header bytes/offsets transcribed directly from `RingHeader.h` (so a producer/reader shared mis-offset still fails the literal-byte assertion). The TRUE cross-language C++↔Python proof (drive the real `spatial_engine_core --input-backend shm:/<name>` against the Python-written ring) is DEFERRED to PR6's 60 s soak (ADR §7.2 / §8 step 6). Confirm with Architect that PR6 owns the cross-language soak so PR5's pure-Python round-trip is sufficient producer proof (vs. adding a C++ build dependency to the adm_player pytest now). — Why it matters: a shared misreading of the spec between `ipc_sink.py` and the in-test reader would pass a same-language test; AC-1's literal-byte pinning + PR6's soak are the two backstops, and if PR6 does NOT own the cross-language proof, PR5 must add option (b)'s cross-repo build dependency. **[iter-2, RATIFIED]** AC-1 extended to pin ALL 15 offsets incl. the unaligned hot-path atomics (closes the static-header half completely); the dynamic index/wrap/planar arithmetic (AC-3/4/5) proves producer↔reader self-consistency only, and AC-9 proves publish PROGRAM-order only (the GIL makes in-process memory-ordering detection vacuous — Critic C1). PR6's soak is the SOLE cross-language proof of both the dynamic arithmetic and the weak-memory ordering. PR5's pure-Python round-trip is sufficient PRODUCER-LOGIC proof.
- [ ] **PR5-Q2 (Decision 2 — drain-dwell coupled to PR4's 1 Hz tick; discharges PR4-Q6 option a):** PR5 discharges PR4-Q6 via option (a): `IpcRingSink.close()` does Streaming→Draining, dwells `drain_dwell_s` (default **1.0 s** = one PR4 engine-tick period, PR4 D4), then →Closed, so the engine's 1 Hz telemetry tick observes `Draining(2)` distinctly from `Closed(3)`. The 1.0 s default is COUPLED to PR4's 1 Hz cadence (PR4-Q5); if PR4's tick cadence ever changes, this dwell must track it (e.g. ≥ the new tick period). A `drain_dwell_s=0.0` knob exists for fast-exit callers/tests and accepts PR4-Q6 option (b) best-effort (terminal `Closed` only). Confirm the 1.0 s default + the coupling note with Architect. — Why it matters: a hard-coded dwell that drifts from PR4's cadence silently re-opens PR4-Q6 (Draining under-observed); the coupling must be documented so a future PR4 cadence bump updates both.
- [ ] **PR5-Q3 (Decision 3 — `ipc://<name>` POSIX-name semantics + ADR §4.1 example reconciliation):** `--sink ipc://<name>` is parsed as a POSIX shm NAME → `multiprocessing.shared_memory.SharedMemory(name=<name>)` → `/dev/shm/<name>` (verified on this host); the engine pairs via `--input-backend shm:/<name>` (leading slash, no second slash → the C++ consumer's `is_posix_shm_name` POSIX branch, verified `SharedMemoryRegion.cpp:30-39`). The ADR §4.1 example `ipc:///tmp/spe-session-1.shm` (a FILESYSTEM PATH) does NOT compose with `multiprocessing.shared_memory` (no arbitrary-path create) and would re-open the `O_NOFOLLOW`/symlink surface PR3-Q7/PR4-D5 deferred; it is reconciled to the name form `ipc://spe-session-1`. Confirm Architect ratifies the name semantics + the `shm:/<name>` engine pairing + the ADR §4.1 example edit. — Why it matters: the `--sink`↔`--input-backend` pairing is the operator contract PR6's soak and end users depend on; a path-vs-name mismatch means the engine opens the wrong object (or nothing).
- [ ] **PR5-Q4 (Decision 5 — `--block-size` default 256 vs `--block-frames` 512 + engine-block divisor):** PR5 adds a new `--block-size` (default **256**, ADR §4.1) as the shm wire `block_size` (the producer's write granularity, ADR §2.3:75), distinct from the existing `--block-frames` (default 512, the device-path audio/OSC read block). In shm mode the read loop feeds the sink in `block_size`-frame chunks. The C++ consumer validates `block_size` divides the engine block (PR2-Q7 `BlockConfigMismatch`); 256 divides a 256/512 engine block. Confirm 256 (vs reusing 512) and the divisor relationship the operator must respect. — Why it matters: a `block_size` that doesn't divide the engine block makes the engine refuse to attach (PR2-Q7); two block knobs need a clear contract so the operator doesn't mismatch them.
- [ ] **PR5-Q5 (Decision 6 — corrected two-repo commit boundary; briefing premise was wrong):** VERIFIED git topology: adm_player is its OWN git repo at `/home/seung/mmhoa/adm_player/dreamscape` (tracks `adm_player/*.py`); the outer `/home/seung` repo (531 dirty unrelated files) does NOT track the dreamscape source; spatial_engine is a third repo. **[iter-2 correction, Critic MAJOR]** the iter-1 "clean, HEAD `b58f827`" sub-claim was FALSE — at review the dreamscape repo had 3 unrelated dirty `adm_recorder` files, which a parallel session committed as HEAD `6b9534d` mid-review. The tree is VOLATILE (concurrent sessions touch it), so the scoped-add discipline + a `status --short` re-check before staging is MANDATORY in dreamscape too, not just in `/home/seung`. The original task briefing claimed adm_player's git root is `/home/seung` and the code commits there — that is FACTUALLY WRONG. PR5 produces TWO commits: code+test in the `dreamscape` repo (scoped `git -C .../dreamscape add <4 paths>`), plan+open-questions [+ optional ADR §4.1 edit] in the `spatial_engine` repo. NEVER `git add -A` (the `/home/seung` 530-dirty-file footgun, PM8). Confirm Architect ratifies the corrected boundary. — Why it matters: committing the producer code into the wrong (`/home/seung`) repo either no-ops (it doesn't track the source) or, with `git add -A`, sweeps 530 unrelated files into one commit — a catastrophic, hard-to-unwind mess.

## v0.8 audit P3 spinoff — 2026-05-29

- [x] **DSP-6 (v0.8 audit P3.3 spinoff, 2026-05-29) — FdnReverb effective delay is 1 sample regardless of `kBaseDelaysSamples[i]`** — **RESOLVED 2026-05-29 by P2.3.** `FdnReverb.cpp:90-99` now reads `delayOut[i] = dl.buf[dl.writePos]` (oldest slot pre-overwrite), restoring the intended D_i-sample delay. T60 oracle reinstated as `test_p7_fdn_t60_accuracy` (peak-RMS reference, ±30 % tolerance) — measured T60 for `feedback ∈ {0.5, 0.7, 0.9}` matches D_mean formula within 0.6–15.4 %. `test_p7_fdn_decay` rewritten with kBlock=4096 to absorb the early-reflection cluster (the broken pre-fix code passed it only because the signal decayed within one 64-sample block). NO_JUCE ctest 101/101 green, pytest 225/4-skip unchanged.

  While implementing the P3.3 T60 accuracy oracle test, the closed-form expected T60 (derived from configured `feedback` and mean delay length via `T60 = -3·D_mean/(fs·log10(g))`) mismatched the measured T60 by 100×+. Root cause: `FdnReverb.cpp:92` reads `readPos = dl.writePos - 1`, i.e. the most-recently-written sample, instead of `readPos = dl.writePos` (which after the `writePos++` / wrap-around increment points to the OLDEST sample, giving a true `dl.length`-sample delay). The `dl.length` field controls buffer allocation only — it is never used as the read offset. As a result the FDN acts as a 1-sample-delay feedback loop with Hadamard mixing: it produces amplitude decay (feedback < 1) but NOT the dense reverberant tail a feedback delay network is designed to produce.

  **Evidence:** Sample-by-sample probe (feedback=0.99, damping=0): output at sample 1 = 1.0, sample 3 ≈ 0.980 = 0.99², sample 5 ≈ 0.961 — geometric decay at exactly the per-sample rate, confirming ~1-sample effective delay. With 512-sample block processing and FTZ/DAZ, the signal reaches float denormal range within the first block (≤85 ms for default feedback=0.98) and is flushed to zero by the SSE MXCSR bits enabled in `prepareToPlay()`, explaining the -384 dB collapse observed in the block-mode probe.

  **Existing tests pass vacuously:** `test_p7_fdn_decay` asserts only monotone RMS decrease (satisfied by any decaying signal). `test_p7_fdn_idle_denormal` asserts `abs_max < 1.0` after a single impulse + 64 blocks of silence (trivially satisfied once the signal is flushed by FTZ). Neither test pins the time constant.

  **Proposed fix (one line):** Change `FdnReverb.cpp:92` from:
  ```cpp
  int readPos = dl.writePos - 1;
  if (readPos < 0) readPos += dl.length;
  ```
  to:
  ```cpp
  int readPos = dl.writePos;   // writePos was just incremented; this is the oldest slot
  if (readPos >= dl.length) readPos = 0;
  ```
  Wait — `writePos` is incremented AFTER the write (`dl.writePos++` at line 121), so at read time (line 92) `writePos` still points to the NEXT-to-write slot, which IS the oldest slot after a full revolution. The correct read is therefore `readPos = dl.writePos` (no `-1`), with the same bounds check.

  **Resolution:** Open new DSP correctness phase **P2.3**: one-line fix to `readPos = dl.writePos` (+ bound wrap), reinstate `test_p7_fdn_t60_accuracy.cpp` from the deferred state, re-run ctest. Sequence after the P3-subset commit (P3.2/P3.4/P3.7) so the test-addition batch lands cleanly first.

  **Scope note:** P3.3 test file (`test_p7_fdn_t60_accuracy.cpp`) was authored but deliberately NOT committed with this batch — it will be reinstated alongside the P2.3 production fix so the test and fix land in the same commit.

## spatial-engine-v0.9-laneA-metrics-dashboard - 2026-05-29

- [ ] **A9-Q1 (DD-A, blocks A-M1)**: `/sys/metrics` 신규 채널 vs 기존 `/sys/state` trailing-args. Plan 권장은 신규 `/sys/metrics`(ObservabilityCounters.h:6 이미 예약; null/dante 경로에서도 상시 방출 필요; 기존 `/sys/state`는 ShmTelemetryEmitter shm-전용 on-change라 의미충돌). — Why it matters: 잘못 선택하면 null/dante 백엔드에서 CPU/xrun이 안 나가 acceptance(AC1·상시표시) 위반.
- [x] **A9-Q2 (DD-A, OSC encoder signature) — RESOLVED 2026-05-30 → `,s` key=value**: `/sys/metrics` 인코딩은 기존 3-arg `sendReply(addr,",s",kv)` overload(OSCBackend.h:120) 재사용 = `ShmTelemetryEmitter::emitState`(`/sys/state`) 선례 그대로. 신규 C++ 인코더·신규 ADR signature 결정 없음. 멀티-int `,iiiii`(encodeOscReplyMetrics) 안은 드롭. — Why it matters(resolved): 회귀 표면·ADR 부담 최소화.
- [ ] **A9-Q3 (DD-B, A-M1)**: CPU% 측정 clock 소스 — steady_clock wall(권장, 이식성·xrun 직결) vs CLOCK_THREAD_CPUTIME_ID(정확한 CPU 점유율, macOS 분기 부담). 집계는 EWMA(α≈0.1) + **스칼라 러닝 p99 추정기**(단일 atomic, P²/GK 류; 1024-슬롯 reservoir 는 RT coherence 위해 드롭, user-approved 2026-05-30). — Why it matters: wall은 프리엠션 포함해 약간 비관적이나 "deadline 준수" 운영 관심사에 더 actionable; 스칼라 추정기는 cross-thread race-free.
- [ ] **A9-Q4 (DD-C, A-M4 — 사용자/Architect 확인 필요)**: 대시보드 차트 라이브러리. 윤곽 문서는 Chart.js로 적혀있으나 원칙 4(오프라인·외부 의존성 0; static/에 vendored lib 없음 확인)와 충돌. Plan 권장은 자체 경량 canvas 미니차트(~150 LOC). uPlot은 시계열 확장 시 승격 후보. — Why it matters: 의존성 도입 = 오프라인 운영·번들·playwright mount 검증 복잡도 증가.
- [ ] **A9-Q5 (DD-D 정책 확정)**: WS `/ws/metrics` fan-out backpressure — Plan 명문화는 latest-wins(큐 사이즈 1, drop-newest, send 실패 시 즉시 disconnect). 1Hz 저레이트라 충분 판단. — Why it matters: 다중 탭/느린 클라이언트에서 메모리 누적 방지.
- [ ] **A9-Q6 (follow-up, scope 확인)**: `/sys/binaural_status` 1Hz push 신설 여부. 윤곽 A.0는 기성 채널로 전제하나 현재 미방출(demote 상태는 BinauralMonitor atomic에만 존재). A-M1 `/sys/metrics`에 demote_count만 포함하면 A.1 §3 binaural 패널 최소 충족 가능 — 전체 B1/B2 10플래그 status는 별도 milestone로 분리할지. — Why it matters: 윤곽 scope vs 실제 코드 갭; full status 신설 시 A-M1 범위 확대.
- [ ] **A9-Q7 (follow-up, v1.x)**: Windows CPU% clock 분기. 윤곽 A.3가 "Windows 별도 — v1.x"로 못박음. steady_clock 채택 시 Windows 동작은 자연 호환이나 thread-cpu 보조지표는 별도. — Why it matters: 크로스플랫폼 정확도 일관성.
- [ ] **A9-Q8 (follow-up, A-M3/A-M4 panel design — logged 2026-05-30, review CONCERN-3)**: `cpu_peak_pct` (및 `cpu_pct`)는 현재 `CpuMeter::toPct()`가 [0,100]으로 clamp 한다(CpuMeter.h). 즉 한 블록이 자기 budget을 초과(wall > budget, near-xrun 과부하)해도 100%로 캡핑되어 **건강한 100%와 구분 불가** — 운영상 가장 흥미로운 이벤트(데드라인 초과)가 숨겨진다. A-M1에서는 clamp를 의도적으로 유지(wire/range 계약·기존 테스트 [0,100] 가정 보존). A-M3/A-M4 패널 설계 시 결정 필요: (a) uncapped ratio(예: cpu_ratio_x1000 또는 별도 `cpu_max_ratio` 필드)를 추가 방출할지, (b) 100% 도달 시 별도 overload/near-xrun 플래그를 surface할지. — Why it matters: clamp 유지 시 대시보드가 과부하 임박을 시각화 못 함; 새 필드 도입 시 `/sys/metrics` 와이어 확장 + UI 패널 범위 확대.

## v0.9 Lane B — HRTF Dataset Diversification - 2026-05-30 (REV 3)
- [x] **B-M3 (CLOSED REV2)**: apply pending SOFA on the ~1 Hz control tick (`core/src/bin/spatial_engine_core.cpp:628`) alongside `applyPendingAmbiDecoderChange()` — live swap, off audio thread, ≥1-block cadence preserved.
- [x] **B-M2 crossfade (CLOSED REV2)**: B1 self-heals via the existing per-object 2-block crossfade (`BinauralMonitor.cpp:146–168`) once the table/tree slot flips; no extra click-suppression needed.
- [x] **B-M2 B2 safety (RESOLVED REV3)**: bare `effectiveMode()` quiesce gate was a TOCTOU race (audio thread re-promotes B2 at `BinauralMonitor.cpp:428`) → mandatory `vs_rebuild_in_progress_` single-writer handshake; audio force-Directs at the C1 snapshot (`:755` / `SpatialEngine.cpp:796`) while rebuild in progress. Relacy models it with a dedicated force-Direct negative control.
- [ ] B-M1: JSON parser already in C++ deps vs hand-roll fixed schema — avoid adding nlohmann/json for a 4-entry catalog (dependency footprint).
- [ ] B-M2 (B2 perception, still open): momentary Direct flash on a B2-active swap acceptable, or take the deferred full B2 VS double-buffer (`active_vs_slot_`, no TOCTOU) in-scope? — perceptual call, not safety.
- [ ] Lane B Risk #3: recommended "reset demote on swap, keep 60s user-reset cooldown clock" (strike detector re-accrues continuously; cooldown prevents swap-spam reset bypass) — Critic to confirm.

## spatial-engine-v0.9 Lane F4 (REV2) - 2026-06-02
- [ ] **F4-Q1 (F4b save-source)**: Confirm Option B2 (snapshot SpatialEngine::obj_cache_ on the control thread) over B1 (route through StateModel) — StateModel is bypassed for ADM-OSC object traffic (ADR 0006), lacks width/reverb_send, and reviving it as authoritative reintroduces the seq=0 drop bug. — Why it matters: picking the wrong source either reintroduces a closed bug or persists a divergent/partial mirror.
- [ ] **F4-Q2 (cross-thread read acceptability)**: Confirm the bounded benign read of audio-thread-owned obj_cache_ from the control thread is acceptable (matches existing objCacheActiveAt idiom), with atomic-scalar/control-tick hardening deferred to Follow-up. — Why it matters: determines whether F4b needs atomic widening now or later.
- [ ] **F4-Q3 (param-7 reject fold-in)**: Confirm folding the /obj/dsp param-7 decoder reject (CommandDecoder.cpp:555) INTO this lane vs deferring. Today param 7 mis-routes to EQ band 0 (silent corruption), not a drop. — Why it matters: a width-via-dsp client silently corrupts EQ until fixed.
- [ ] **F4-Q4 (second JSON writer)**: Re-confirm no other "objects":[ scene-snapshot JSON writer exists besides C++ SceneSnapshot::toJson before merge. — Why it matters: an un-updated writer would still drop the new fields.

## spatial-engine-v0.9 Lane F4 (REV3) - 2026-06-02
- [x] **F4-Q1 (CLOSED REV3)**: F4b source = snapshot obj_cache_ (B2), StateModel/B1 invalidated (bypassed for ADM-OSC per ADR 0006). Accepted by Architect.
- [ ] **F4-Q5 (concurrency mechanism, NEW REV3)**: Confirm Option 2d (publish-on-dirty double-buffer + seqlock generation, F5/ADR 0021 idiom) over 2b (atomicize ObjCache scalars). 2b rejected primary: forces .load() across dozens of RT renderer read sites (SpatialEngine.cpp:647-934) and gives only per-field, not per-object, consistency. 2a (TSan suppression) off the table — would be the only suppression in a 0-races project. — Why it matters: wrong choice either ripples into the RT hot path or fails the project's zero-races TSan/Relacy gate.
- [ ] **F4-Q6 (mute persistence, NEW REV3)**: Confirm the `touched` heuristic (emit object iff active OR any captured field is non-default) for mute persistence is acceptable with its one documented corner (object driven to exact defaults then muted → treated as untouched/omitted), vs adding a `valid` flag to ObjCache now. — Why it matters: ObjCache has no valid flag (SpatialEngine.h:387-398); mute state would otherwise be lost on save.
- [ ] **F4-Q3 (param-7 reject fold-in, carried)**: Confirm folding /obj/dsp param-7 decoder reject into this lane. Param 7 currently mis-routes to EQ band 0 (silent corruption).
- [ ] **F4-Q4 (second JSON writer, carried)**: Re-confirm no other "objects":[ scene-snapshot JSON writer exists besides C++ SceneSnapshot::toJson before merge.

## spatial-engine-v0.9 Lane F4 (REV4) - 2026-06-02
- [x] **F4-Q5 (RESOLVED REV4)**: Concurrency = Option 2d publish-on-dirty THREE-buffer rotation (race-free by construction for an unbounded-rate writer). REV3's two-buffer even/odd seqlock was livelock-prone and replaced (Architect amendment A1). 2b/2a remain rejected.
- [ ] **F4-Q5b (confirm REV4 three-buffer)**: Architect to confirm the three-buffer writer rotation (skip published index, then advance) + reader bounded single re-check is correct, and that AC9 (soak_scene_save_race) CONFIRMS rather than solely guarantees it. — Why it matters: the two-buffer form was a real defect; three-buffer is the fix.
- [x] **F4-Q-A2 (RESOLVED REV4)**: RT-assert macro corrected to SPE_RT_NO_ALLOC_SCOPE() (RtAssertNoAlloc.h:54); the existing scope guard at audioBlock entry (SpatialEngine.cpp:464) already covers the publish — no new macro call. Gated on -DSPATIAL_ENGINE_RT_ASSERTS (RT-assert/TSan build).
- [x] **F4-Q-A3 (RESOLVED REV4)**: dirty=popped_any is DELIBERATELY coarse; dangerous mode is UNDER-publish (eliminated), over-publish harmless. Publish runs POST-drain so SysReset's obj_cache_.fill() (SpatialEngine.cpp:508) is captured.
- [ ] **F4-Q6 (mute touched-heuristic, carried)**: Confirm touched heuristic for mute persistence with its one documented corner vs adding a valid flag to ObjCache.
- [ ] **F4-Q4 (second JSON writer, carried)**: Re-confirm no other "objects":[ scene-snapshot JSON writer exists besides C++ SceneSnapshot::toJson before merge.

## spatial-engine-v0.9 Lane F4 (REV5) - 2026-06-02
- [x] **F4-C1 (RESOLVED REV5)**: Reader single-bounded-re-check was still racy (no re-check after the 2nd copy → lap during deschedule). Replaced with a retry-until-stable seqlock loop; liveness = reader copy « 2 audio-block periods. Kept 3 buffers (no need for 4).
- [x] **F4-M1 (RESOLVED REV5)**: AC9's "az from N / dist from ≤N" was unprovable (ObjCache has no gen/seq field). Replaced with a correlated-field-pair invariant (writer sets az=k, width=encode(k); reader asserts width==encode(az)), structurally detecting cross-buffer tearing, plus TSan 0-races, ≥150 rounds.
- [x] **F4-M2 (RESOLVED REV5)**: Mute corner bounded — ObjMove sets active=true (SpatialEngine.cpp:495-496), so any positioned object is touched; corner is only objects driven to FULL defaults then deactivated (vanishingly rare).
- [x] **F4-item (RESOLVED REV5)**: published_index_/snap_buf_/writer_next_ persist across prepare/stop-restart; stale published_index_>=0 is benign (points at still-valid state); SysReset's obj_cache_.fill() is captured by the dirty post-drain publish.
- [x] **F4-item (RESOLVED REV5)**: No assumed popped_any — executor declares `bool cache_dirty=false;` before the drain loop (verified none exists at SpatialEngine.cpp:488-491), set true at top of loop body before the OOB continue.
- [x] **F4-item (RESOLVED REV5)**: Two width write paths documented — ObjWidth verb (:161/:605-607) live; ObjDsp case 7 (:579) dead until F4b-T0; both write c.width_rad, snapshot captures either.
- [ ] **F4-Q4 (carried, pre-merge)**: Re-confirm no other "objects":[ scene-snapshot JSON writer exists besides C++ SceneSnapshot::toJson before merge.
